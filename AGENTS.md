# AGENTS.md - 项目开发规范

> 本规范适用于所有在此仓库中运行的 AI 代理
>
> 核心原则：**先想清楚，再写代码**

---

## 一、核心工作流程：七步思考法则

> 就像医生看病一样，做任何事情之前都要走完这七步

### 第一步：停下来想想（暂停反思）

- 这个任务到底是什么？
- 用户真正想要的是什么？
- 有没有什么我可能误解的地方？

### 第二步：找找线索（收集证据）

- 看看相关的代码文件
- 找找类似的例子
- 了解现有的实现方式

### 第三步：挖到根源（根因分析）

> 像拔萝卜一样，要把根都拔出来，不能只处理表面

- 问题的真正原因是什么？
- 不是头痛医头，脚痛医脚
- 找到最根本的问题点

### 第四步：想出办法（方案设计）

> 像画房子的图纸一样，先设计好再动手

- 列出所有可能的解决方案
- 每个方案的优缺点是什么
- 哪个方案最适合当前情况

### 第五步：检查方案（方案审视三步法）

> 像考试前检查试卷一样，确保没有遗漏

1. **完整性检查**：这个方案能解决所有问题吗？
2. **风险评估**：这样做会不会带来新问题？
3. **可行性验证**：现有条件能实现这个方案吗？

### 第六步：动手实施（执行修复）

> 像按照菜谱做菜一样，一步一步来

- 先做最重要的部分
- 每做完一步就检查一下
- 遇到问题及时调整

### 第七步：确认效果（验证结果）

> 像厨师尝味道一样，确保做出来的菜是好吃的

- 测试修改是否有效
- 确认没有引入新问题
- 记录这次经验以便下次参考

---

## 二、问题处理规范

### 2.1 遇到问题的处理流程

1. **先全面了解**：收集相关信息，不急着动手
2. **分析根本原因**：找到问题源头，不只解决症状（参考七步法则第三步）
3. **制定方案**：准备多个方案，评估利弊（参考七步法则第四、五步）
4. **验证方案**：小范围验证后再推广
5. **总结经验**：记录问题和解决方案

### 2.2 禁止行为

- ❌ 未经沟通自行降级方案
- ❌ 遇到卡点悄悄绕过
- ❌ 自己猜测然后硬做
- ❌ 复制粘贴代码不理解原理
- ❌ 留下 TODO 但不跟进
- ❌ 头痛医头，脚痛医脚（不找根本原因）

### 2.3 需要确认的情况

遇到以下情况，必须先问用户：

- 需要做影响多个模块的改动
- 技术选型有多种方案
- 需要外部依赖或第三方服务
- 涉及数据迁移或不可逆操作

---

## 三、代码规范

### 3.1 总体风格

- **低耦合**：模块之间尽量减少依赖，接口隔离
- **简洁**：避免过度设计，不造轮子
- **中文注释**：关键逻辑必须用中文详细注释

### 3.2 具体规则

| 语言 | 规范 |
|------|------|
| JavaScript/TypeScript | 函数式编程，优先使用箭头函数 |
| React | Hooks + Functional Component，不使用 class 组件 |
| Python | PEP 8 规范，类型提示 |
| Rust | 遵循官方 style guide，优先使用 iterators |
| Go | 标准库优先，避免过度封装 |

### 3.3 注释规范

```javascript
// ✅ 正确：说明"为什么"而不是"做了什么"
// 为了解决用户登录后token过期的问题，使用refresh token机制
async function refreshToken() { ... }

// ❌ 错误：冗余注释
// 这是一个刷新token的函数
async function refreshToken() { ... }
```

---

## 四、Git 规范

### 4.1 分支策略（三层分支）

```
main (或 master)  →  生产环境
    ↑
    ├── develop        →  开发主干
    │       ↑
    │       ├── feature/xxx    →  功能分支
    │       ├── fix/xxx        →  修复分支
    │       └── refactor/xxx    →  重构分支
    │
    └── release/x.x.x  →  发布分支
```

### 4.2 提交规范

- **简洁提交**：一次提交只做一件事
- **提交信息格式**：
  ```
  [类型] 简短描述
  
  详细说明（可选）
  ```

- **类型前缀**：
  - `feat`: 新功能
  - `fix`: 修复 bug
  - `docs`: 文档更新
  - `refactor`: 重构
  - `test`: 测试
  - `chore`: 构建/工具

### 4.3 示例

```
[feat] 添加用户登录功能

- 实现用户名密码登录
- 添加 token 刷新机制
- 集成验证码登录

Closes #123
```

---

## 五、测试规范

### 5.1 测试金字塔

> 像盖房子一样，先打好地基，再盖楼层

```
        /\
       /  \      E2E 测试 (少量) - 屋顶
      /____\
     /      \
    / 集成测试 \   (中等) - 楼层
   /__________\
  /            \
 /   单元测试    \  (大量) - 地基
/________________\
```

### 5.2 测试顺序

1. **先自动化测试**
   - 优先编写单元测试和集成测试
   - 确保核心逻辑有测试覆盖

2. **后 UI 测试**
   - UI 测试放在最后
   - 使用 Playwright / Cypress 等工具

### 5.3 测试要求

- 新功能必须有测试覆盖
- Bug 修复必须添加回归测试
- 测试必须能独立运行，不依赖外部状态

---

## 六、项目结构

### 6.1 通用结构

```
src/
├── components/     # UI 组件
├── hooks/          # 自定义 Hooks
├── utils/          # 工具函数
├── services/       # 业务逻辑
├── types/          # 类型定义
├── constants/      # 常量
└── index.ts        # 入口导出
```

### 6.2 命名约定

| 类型 | 规则 | 示例 |
|------|------|------|
| 文件 |  kebab-case | user-service.ts |
| 组件 | PascalCase | UserProfile.tsx |
| 函数 | camelCase | getUserInfo() |
| 常量 | UPPER_SNAKE | MAX_RETRY_COUNT |
| CSS 类 | kebab-case | .user-profile |

---

## 七、沟通规范

### 7.1 交互方式

- **一句话指令**：用户用自然语言描述需求，AI 执行
- **透明进度**：让用户知道当前状态和下一步
- **主动确认**：关键节点必须用户确认后才能继续

### 7.2 回复要求

- **详细全面**：包含思考角度、可能性、利弊权衡
- **问题导向**：先分析问题，再给出解决方案
- **举一反三**：解决一个问题时，预判类似问题并一并处理

---

## 八、依赖管理

### 8.1 原则

- **不造轮子**：优先使用成熟的开源库
- **评估后选**：根据项目需求选择最适合的
- **版本控制**：锁定依赖版本，避免自动升级

### 8.2 常用选型

| 场景 | 推荐方案 |
|------|----------|
| 前端框架 | React + TypeScript |
| 后端 API | Express / Fastify / NestJS |
| 数据库 | PostgreSQL / MongoDB |
| 样式方案 | Tailwind CSS / CSS Modules |
| 状态管理 | Zustand / Redux Toolkit |
| HTTP 客户端 | Axios / Fetch |
| 测试框架 | Vitest / Jest / Playwright |

---

## 九、文档规范

### 9.1 必须文档化的内容

- API 接口说明
- 数据库表结构
- 关键业务逻辑
- 部署流程
- 配置说明

### 9.2 文档位置

- 代码同目录：`README.md`
- 设计文档：`docs/` 目录
- API 文档：`openapi.yaml` 或 `swagger`

---

## 十、部署规范

### 10.1 部署前检查

- [ ] 所有测试通过
- [ ] 代码已审查
- [ ] 环境变量配置正确
- [ ] 依赖版本锁定
- [ ] 回滚方案准备

### 10.2 部署原则

- 小步迭代，避免大规模变更
- 灰度发布，逐步放量
- 监控告警必须配置

---

## 十一、工具链

### 11.1 开发工具

| 用途 | 工具 |
|------|------|
| 包管理 | npm / yarn / pnpm |
| 代码检查 | ESLint |
| 代码格式化 | Prettier |
| 构建工具 | Vite / Webpack |
| 版本管理 | Git |

### 11.2 常用命令

```bash
# 安装依赖
pnpm install

# 运行开发服务器
pnpm dev

# 运行测试
pnpm test

# 代码检查
pnpm lint

# 构建生产版本
pnpm build
```

---

## 十二、检查清单

每次提交前必须确认：

- [ ] 完成了七步思考法则
- [ ] 代码符合上述规范
- [ ] 添加了必要的注释
- [ ] 运行了相关测试
- [ ] 没有留下 console.log 或调试代码
- [ ] 提交信息格式正确

---

## 十三、代码审查要点

> 像给试卷批改一样，仔细检查每一行

### 13.1 必须检查

1. **逻辑正确性**：代码能解决实际问题吗？
2. **边界处理**：有没有考虑极端情况？
3. **性能影响**：会不会有性能问题？
4. **安全性**：有没有安全漏洞？
5. **可维护性**：别人能看懂吗？

### 13.2 常见问题

| 问题 | 后果 | 解决办法 |
|------|------|----------|
| 硬编码 | 难以维护 | 使用配置或常量 |
| 重复代码 | 难以修改 | 提取为公共函数 |
| 深层嵌套 | 难以阅读 | 拆分函数 |
| 魔法数字 | 难以理解 | 使用命名常量 |

---

> 本规范将根据项目实践持续迭代更新
